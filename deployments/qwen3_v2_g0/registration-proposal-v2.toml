schema_version = 2
name = "OPD"
enabled = false
integration_status = "protocol-v2-disabled-qwen3-v2-elastic-g0-proposal"

[paths]
code_root = "/home/del6500/projects/OPD"
data_root = "/data/del6500/OPD"
scratch_root = "/scr/del6500/OPD"
proposed_handoff_root = "/scr/del6500/OPD/scheduler"

[policy]
allow_cpu_jobs = true
allow_gpu_jobs = true
allow_process_signals = false
allow_project_file_mutation = false

[integration]
entrypoint = "/home/del6500/projects/OPD/scripts/server_scheduler/opd-entrypoint"
allowed_tasks = ["repository_preflight", "qwen3_v2_g0", "qwen3_v2_gpu_preflight"]
notes = "Disabled OPD successor proposal for independent central review. Normal requests contain only workflow_id, plan_sha256, and unit_id and omit execution_profile and resources. qwen3_v2_g0 and qwen3_v2_gpu_preflight each expose one scheduler-managed profile; 1/2/3/4 GPUs are central claim-time allocation candidates, never project task variants or request fields. Their handlers derive the concrete world size only from the running manifest and scheduler environment, preserve CUDA_VISIBLE_DEVICES, and use logical devices 0..N-1. The elastic G0 protocol consumes one world-size-independent 64-slot logical batch from an ordered population of 256 unique prompts per optimizer update, preserves the exact 2,000,000 global nonpadding-input-token budget and 120-step ceiling, stores rank-local trainer counters separately, supports same-world checkpoint resume, and rejects changed-world checkpoint loads before state restoration. Count-specific real validation evidence belongs to one fingerprinted execution class and is reusable by later scientific jobs whose safety fingerprint is identical; it is not regenerated per experiment. Candidate E v1's accepted four-pilot first-G0 gate remains historical authority until a new proposed successor amendment, certification, and Candidate E science protocol are independently accepted. The conservative one-GPU cold-start estimates are unmeasured and will be superseded by central per-count observations. This file remains disabled and authorizes neither central installation nor enablement, submission, or execution."

[[tasks]]
name = "repository_preflight"
selection_policy = "earliest_finish"

[[tasks.execution_profiles]]
name = "repository-preflight-cpu"
kind = "cpu"
resource_mode = "fixed"
scheduling_goal = "latency"
cpu_cores_min = 1
cpu_cores_preferred = 1
cpu_cores_max = 1
memory_mib = 128
gpu_count = 0
gpu_memory_mib = 0
gpu_utilization_pct = 0
gpu_exclusivity = "shareable"
gpu_models = []
estimated_runtime_seconds = 30.0
cpu_scaling_efficiency = 0.0

[[tasks]]
name = "qwen3_v2_g0"
selection_policy = "earliest_finish"

[[tasks.execution_profiles]]
name = "qwen3-v2-g0-elastic"
kind = "gpu"
gpu_count_policy = "scheduler"
resource_mode = "fixed"
scheduling_goal = "latency"
cpu_cores_min = 24
cpu_cores_preferred = 24
cpu_cores_max = 24
memory_mib = 196608
gpu_memory_mib = 81920
gpu_utilization_pct = 95
gpu_exclusivity = "required"
gpu_models = ["NVIDIA RTX PRO 6000 Blackwell Server Edition"]
estimated_runtime_seconds = 86400.0
cpu_scaling_efficiency = 0.0

[[tasks]]
name = "qwen3_v2_gpu_preflight"
selection_policy = "earliest_finish"
preflight_gpu_count_constraint = true

[[tasks.execution_profiles]]
name = "qwen3-v2-gpu-preflight-elastic"
kind = "gpu"
gpu_count_policy = "scheduler"
resource_mode = "fixed"
scheduling_goal = "latency"
cpu_cores_min = 24
cpu_cores_preferred = 24
cpu_cores_max = 24
memory_mib = 196608
gpu_memory_mib = 81920
gpu_utilization_pct = 95
gpu_exclusivity = "required"
gpu_models = ["NVIDIA RTX PRO 6000 Blackwell Server Edition"]
estimated_runtime_seconds = 7200.0
cpu_scaling_efficiency = 0.0
